import { Button, Input, Link } from "@/components/ui";

const UIPage = () => {
  return (
    <div>
      <Button />
      <Link href="/" />
      <Input />
    </div>
  );
};

export default UIPage;
